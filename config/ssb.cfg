{feed_store_location, "{{feed_store_location}}"}.

{blob_store_location, "{{blob_store_location}}"}.

{network_id, "{{network_id}}"}.

{peer_dialer, "{{peer_dialer}}"}.

{room, "{{room}}"}.

{blob_scan, "{{blob_scan}}"}.
